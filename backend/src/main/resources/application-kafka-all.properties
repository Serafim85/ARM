monitoring.collector.enabled=true
# Same JVM runs collector: size pool vs concurrent collectors (override with DB_HIKARI_MAX_POOL_SIZE).
spring.datasource.hikari.maximum-pool-size=${DB_HIKARI_MAX_POOL_SIZE:128}
monitoring.kafka.enabled=true
monitoring.kafka.evaluator-enabled=true
monitoring.kafka.writer-enabled=true
