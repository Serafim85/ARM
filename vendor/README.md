# vendor/

Сторонний код **только для референса**. Не коммитим сюда правки WISLA АРМ.

| Содержимое | Назначение |
|---|---|
| `NetworckScaner-master/` | WISLA NS — источник форка `backend/` + `frontend/` |

Форк: скопировать нужные части в `backend/` и `frontend/`, scan/SNMP/topology не переносить.
